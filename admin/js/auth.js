/**
 * Velora Operations Dashboard - Auth Controller Bridge
 * Authentication routines and session management are handled via ValidationAndAuth.js
 */
(function() {
  'use strict';
  // Expose compatibility placeholder or alias to adminAuthApi if loaded
  if (typeof window !== 'undefined') {
    window.veloraAuthLoaded = true;
  }
})();
